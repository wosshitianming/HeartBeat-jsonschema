package top.kx.heartbeat.infrastructure.persistence.entity.sys;

import java.util.Objects;

/**
 * This class was generated by MyBatis Generator but synthesized here for compile compliance.
 * The actual primary key is a single id column; key class wraps it.
 */
public class SysUserPostDOKey {
    private Long id;

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof SysUserPostDOKey)) return false;
        SysUserPostDOKey that = (SysUserPostDOKey) o;
        return Objects.equals(id, that.id);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id);
    }
}